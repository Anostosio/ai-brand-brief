{
  "name": "ai-brand-brief",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check api/generate.js && node --check lib/brief-core.js"
  },
  "engines": {
    "node": ">=20"
  }
}
